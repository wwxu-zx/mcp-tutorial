[project]
name = "weather-server"
version = "0.1.0"
description = "MCP weather server backed by the US National Weather Service API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.0",
]
