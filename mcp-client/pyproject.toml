[project]
name = "mcp-client"
version = "0.1.0"
description = "Interactive OpenAI client that connects to an MCP server over stdio"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.28.0",
    "openai>=2.43.0",
    "python-dotenv>=1.2.2",
]
